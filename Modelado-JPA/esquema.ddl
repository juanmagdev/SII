CREATE TABLE AUTOR (ID BIGINT NOT NULL, APELLIDOS VARCHAR, FECHANACIMIENTO DATE, INDICEH INTEGER, NOMBRE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE ARTICULO (ID BIGINT NOT NULL, ANIO INTEGER, NUMERO INTEGER, PAGINAFINAL INTEGER, PAGINAINICIAL INTEGER, PUBLICACION VARCHAR, TITULO VARCHAR, VOLUMEN INTEGER, PRIMARY KEY (ID))
CREATE TABLE Autor_FIRMAS (Autor_ID BIGINT, FIRMAS VARCHAR)
CREATE TABLE ARTICULO_AUTOR (Articulo_ID BIGINT NOT NULL, autores_ID BIGINT NOT NULL, PRIMARY KEY (Articulo_ID, autores_ID))
ALTER TABLE Autor_FIRMAS ADD CONSTRAINT FK_Autor_FIRMAS_Autor_ID FOREIGN KEY (Autor_ID) REFERENCES AUTOR (ID)
ALTER TABLE ARTICULO_AUTOR ADD CONSTRAINT FK_ARTICULO_AUTOR_Articulo_ID FOREIGN KEY (Articulo_ID) REFERENCES ARTICULO (ID)
ALTER TABLE ARTICULO_AUTOR ADD CONSTRAINT FK_ARTICULO_AUTOR_autores_ID FOREIGN KEY (autores_ID) REFERENCES AUTOR (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
